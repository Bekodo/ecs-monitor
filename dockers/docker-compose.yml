version: "3"
services:
  ecs-monitor:
    image: ecscputascks
    volumes:
      - ../:/opt/monitor-ecs
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_APP=main.py

    # command: tail -f /var/log/dnf.log
    command: flask run --host=0.0.0.0
